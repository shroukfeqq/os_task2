on:
  push:
     branches:
          - master
    




jobs:
    build:
        runs-on: ubuntu-latest

        steps:
          - name: 'testing print folder contents'
            run: ls
          - name: 'testing printing'
            run: echo hello
